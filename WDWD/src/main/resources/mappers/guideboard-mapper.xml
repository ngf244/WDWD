<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="guideboardMapper">


<!-- 게시글 전체 목록 조회 -->
<select id="listAll" resultType="Guide">
	SELECT
		bno,
		title,
		content,
		writer,
		regdate,
		viewcnt
	FROM
		tbl_board
	ORDER BY
		bno desc,
		regdate desc
</select>


<!-- 게시글 작성 쿼리 -->
<insert id="insert">
	INSERT INTO tbl_board(
		bno,
		title,
		content,
		writer
	) VALUES (
		(SELECT NVL(MAX(bno)+1, 1)FROM tbl_board),
		#{title},
		#{content},
		#{writer}
	)
</insert>

<!-- 게시글 상세보기 조회 -->
<select id="view" resultType="Guide">







</select>
</mapper>